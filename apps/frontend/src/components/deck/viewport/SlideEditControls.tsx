import React from 'react';

interface SlideEditControlsProps {
  isEditing: boolean;
  slideId?: string;
}

// Edit controls moved to ChatPanel
const SlideEditControls: React.FC<SlideEditControlsProps> = () => null;

export default SlideEditControls;
